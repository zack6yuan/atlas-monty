#include "monty.h"
/**
 * nop - does nothing
 * @stack: the stack, nothing will be done to it
 * @line_number: the number of the current line in the file
 * Return: void
*/
void nop(stack_t **stack, unsigned int line_number)
{
        (void)stack;
        (void)line_number;
}